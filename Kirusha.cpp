//Артемьев Кирилл П43021
#include<iostream>
using namespace std;
int main() {
	setlocale(LC_ALL, "Russian");
	/*
	int x;
	cin >> x;
	if (x > 9 && x < 100) {
		int c1;
		int c2;
		c2 = x % 10;
		c1 = x / 10;
		if (c2 % 2 == 0 && (c1 + c2) % 3 == 0)
			cout << "Число" << x << "делится на 6\n";
		else  cout << "Число" << x << " не делится на 6\n";
	}
	else
		if (x > 99 && x < 1000) {
			int c1, c2, c3;
			c3 = x % 10;
			c2 = (x % 100) / 10;
			if (c3 % 2 == 0 && (c1 + c2 + c3) % 3 == 0)
				cout << "Число" << x << "делится на 6\n";
			else  cout << "Число" << x << " не делится на 6\n";
		}
		*/
		/*
		int A;
		cin >> A;
		if (A > 0 && A < 10) {
			cout << "однозначное положительное";
		}
		else if (A > 0 && A < -10) {
			cout << "однозначное отрицательное";
		}
		else if (A > 9 && A < -100) {
			cout << "двухзначное положительное";
		}
		else if (A < -9 && A > -100) {
			cout << "двухзначное отрицательное";
		}
		else if (A > 99 && A < 1000) {
			cout << "трехзначное положительное";
		}
		else if (A < -99 && A > -1000) {
			cout << "трехзначное отрицательное";
		}
		*/
	/*
	int x;
	cin >> x;
	if (x == 12  || x == 1 || x == 2)
		cout << "зима";
	if (x == 3 || x == 4 || x == 5)
		cout << "весна";
	if (x == 6 || x == 7 || x == 8)
		cout << "лето";
	if (x == 9 || x == 10 || x == 11)
		cout << "осень";
		*/
	/*
	int a, b, c;
	cin >> a >> b >> c;
	if (a < b + c && b < a + c && c < a + b) {
		cout << "треугольник";
	else
		cout << " не треугольник";
	}
	*/
	/*
	int A;
	int x1, x2, x3;
	double sum, pr;
	x1 = A / 100;
	x2 = (A % 100) / 10;
	x3 = (A % 100) % 10;
	sum = x1 + x2 + x3;
	pr = x1 * x2 * x3;
	if (A % 2 == 0) {
		cout << sum;
	}
	else if (A % 2 == 1) {
		cout << pr;
	}
	*/
	/*
	int a;
	cin >> a;
	if (a == 1) {
		cout << "Большой";
	}
	if (a == 2) {
		cout << "указательный";
	}
	if (a == 3) {
		cout << "фак";
	}
	if (a == 4) {
		cout << "безымянный";
	}
	if (a == 5) {
		cout << "мизинец";
	}
	*/
	

}

	



